# Postgres
spring.datasource.url=jdbc:postgresql://${DB_ADDR}:${DB_PORT}/${DB_NAME}
spring.datasource.driver-Class-Name=org.postgresql.Driver
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
